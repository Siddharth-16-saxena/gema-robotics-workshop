{
  "name": "gema-education-workshop",
  "version": "1.0.0",
  "description": "AI & Robotics Summer Workshop landing page and enquiry management system",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently -n \"client,server\" -c \"cyan,magenta\" \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "test": "npm run test --prefix client && npm run test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
